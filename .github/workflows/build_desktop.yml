name: Build All Platforms

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-desktop:
    name: Build Desktop (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
    outputs:
      release_tag: v1.0.${{ github.run_number }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Package
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            ./gradlew :desktop:packageMsi :desktop:packageExe
          else
            ./gradlew :desktop:packageDmg
          fi
        shell: bash

      - name: Zip Windows Files
        if: matrix.os == 'windows-latest'
        run: |
          Compress-Archive -Path "desktop/build/compose/binaries/main/msi/*.msi", "desktop/build/compose/binaries/main/exe/*.exe" -DestinationPath "Loomi-Windows-Installers.zip"
        shell: pwsh

      - name: Zip macOS Files
        if: matrix.os == 'macos-latest'
        run: |
          zip -j Loomi-Mac-Installer.zip desktop/build/compose/binaries/main/dmg/*.dmg
        shell: bash

      - name: Upload Temporary Artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.os }}
          path: "*.zip"
          retention-days: 1

  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Handle Google Services File
        run: |
          if [ ! -f "app/google-services.json" ]; then
            echo '{"project_info":{"project_number":"0","project_id":"dummy"},"client":[{"client_info":{"mobilesdk_app_id":"0","android_client_info":{"package_name":"com.echo.loomi"}},"api_key":[{"current_key":"dummy"}]}]}' > app/google-services.json
          fi

      - name: Build Debug APK
        run: ./gradlew :app:assembleDebug

      - name: Rename APK
        run: mv app/build/outputs/apk/debug/app-debug.apk Loomi-Android.apk

      - name: Upload Temporary Artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-android
          path: Loomi-Android.apk
          retention-days: 1

  create-release:
    name: Create Final Release
    needs: [build-desktop, build-android]
    runs-on: ubuntu-latest
    steps:
      - name: Download All Build Files
        uses: actions/download-artifact@v4
        with:
          path: release-files
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v1.0.${{ github.run_number }}"
          name: "Loomi v1.0.${{ github.run_number }}"
          body: "New build for Android, Windows, and macOS."
          draft: false
          prerelease: false
          files: |
            release-files/Loomi-Windows-Installers.zip
            release-files/Loomi-Mac-Installer.zip
            release-files/Loomi-Android.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
